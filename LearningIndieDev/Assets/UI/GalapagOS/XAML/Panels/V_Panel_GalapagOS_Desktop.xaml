<Page x:Name="Panel_Root"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignWidth="1920"
  d:DesignHeight="1080"
  Background="#FF4D5052"
  Foreground="#FFE1E9ED">
  <Page.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../../../GlobalResources.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Page.Resources>
  <Border x:Name="Root_Border">
    <Grid x:Name="ContentGrid" Background="{DynamicResource Salty_SoftGreen}">
      <!-- <ContentControl Width="700" Height="420" Margin="19,330,1201,330" Style="{StaticResource GalapagOS_WindowStyle}">
        <StackPanel Margin="24" VerticalAlignment="Center" Background="#FFDAFF69">
          <TextBlock x:Name="Label1" Text="{Binding DesktopTitle}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource Chrono_TreeBark}" HorizontalAlignment="Center" />
          <TextBlock x:Name="Label2" Text="{Binding WindowTitle}" FontSize="20" Margin="0,16,0,0" Foreground="{DynamicResource Chrono_Olive}" HorizontalAlignment="Center" />
          <TextBlock Text="{Binding StatusText}" Margin="0,24,0,0" Foreground="{DynamicResource Chrono_TreeBark}" HorizontalAlignment="Center" />
          <TextBlock Text="{Binding ViewModelType}" Margin="0,8,0,0" Foreground="{DynamicResource Chrono_Olive}" HorizontalAlignment="Center" />
        </StackPanel>
      </ContentControl>
      <ContentControl Margin="0,0,0,651" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{DynamicResource GalapagOS_WindowStyle}">
        <StackPanel>
          <Ellipse Width="50" Height="50" Fill="#FFF4F4F5" Stroke="#FF000000"/>
          <Rectangle Width="100" Height="100" Fill="#FFF4F4F5" Stroke="#FF000000"/>
          <Ellipse Width="60" Height="60" Fill="#FFF4F4F5" Stroke="#FF000000"/>
        </StackPanel>
      </ContentControl>
      <ContentControl HorizontalAlignment="Center" VerticalAlignment="Center" Style="{DynamicResource GalapagOS_WindowStyle}">
        <TextBlock Text="TextBlock" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF000000" FontSize="48" TextWrapping="Wrap"/>
      </ContentControl>
      <ContentControl Content="ContentControl" Margin="0,0,1182,651" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{DynamicResource GalapagOS_WindowStyle}"/> -->


<Grid x:Name="GalapagOS_Window" Margin="19,0,-19,0">
              <Border x:Name="Window_Root_Border" Width="500" Height="250" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{DynamicResource Chrono_VanillaCream}" BorderBrush="#FF000000" CornerRadius="6,6,6,6">
                <Grid x:Name="WindowContentGrid">
                  <Grid.Background>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                      <GradientStop Color="#FFF3FFB6" Offset="0.9312354"/>
                      <GradientStop Offset="1" Color="#FFCCE43E"/>
                    </LinearGradientBrush>
                  </Grid.Background>
                  <Border x:Name="HeaderBorderContainer" Height="32" VerticalAlignment="Top" Background="#FFDFE99A" CornerRadius="8,8,0,0">
                    <Grid x:Name="HeaderContentGrid">
                      <Grid x:Name="StripeGridContainer">
                        <Rectangle x:Name="Stripe1" Height="2" Margin="0,8,0,0" VerticalAlignment="Top" Fill="{DynamicResource Chrono_Lime}"/>
                        <Rectangle x:Name="Stripe2" Height="2" Margin="0,16,0,0" VerticalAlignment="Top" Fill="{DynamicResource Chrono_Lime}"/>
                        <Rectangle x:Name="Stripe3" Height="2" Margin="0,24,0,0" VerticalAlignment="Top" Fill="{DynamicResource Chrono_Lime}"/>
                        <Button x:Name="CloseButton" Content="Button" HorizontalAlignment="Right" Template="{DynamicResource GalapagOS_CloseWindowButtonStyle}"/>
                      </Grid>
                      <Border x:Name="HeaderTextBorder" HorizontalAlignment="Left" CornerRadius="4,2,4,0">
                        <Border.Background>
                          <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1" Opacity="1">
                            <GradientStop Offset="0" Color="#FFA9AA5A"/>
                            <GradientStop Offset="1" Color="#FFEDFFA0"/>
                          </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                          <DropShadowEffect Color="#1FB5B67D" BlurRadius="6" Direction="325" ShadowDepth="3"/>
                        </Border.Effect>
                        <TextBlock x:Name="HeaderText" Text="GalapagOS Window" Height="24" Margin="6,0,6,0" Foreground="{DynamicResource GalapagOS.Window.HeaderForeground}" StrokeThickness="2" FontSize="20" FontWeight="Bold" TextAlignment="Center"/>
                      </Border>
                    </Grid>
                  </Border>
                </Grid>
              </Border>
              </Grid> 
              </Grid> 
              </Border> 
                
</Page>
