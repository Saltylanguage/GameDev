<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <!-- Semantic colors mapped from the Figma pilot. -->
    <SolidColorBrush x:Key="Ui.Brush.Canvas" Color="#FF0A1720" />
    <SolidColorBrush x:Key="Ui.Brush.Header" Color="#FF152B36" />
    <SolidColorBrush x:Key="Ui.Brush.Window" Color="#FF1B2A38" />
    <SolidColorBrush x:Key="Ui.Brush.WindowRaised" Color="#FF263F4B" />
    <SolidColorBrush x:Key="Ui.Brush.TextPrimary" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="Ui.Brush.TextSecondary" Color="#FFD9E7F0" />
    <SolidColorBrush x:Key="Ui.Brush.TextAccent" Color="#FFF1C27D" />
    <SolidColorBrush x:Key="Ui.Brush.Success" Color="#FF8FE3A8" />
    <SolidColorBrush x:Key="Ui.Brush.Danger" Color="#FFFF9D9D" />

    <!-- Scalar tokens stay independent; composite padding values are derived below. -->
    <sys:Double x:Key="Ui.Space.100">8</sys:Double>
    <sys:Double x:Key="Ui.Space.150">12</sys:Double>
    <sys:Double x:Key="Ui.Space.200">16</sys:Double>
    <sys:Double x:Key="Ui.Space.300">24</sys:Double>
    <CornerRadius x:Key="Ui.Radius.Control">8</CornerRadius>
    <CornerRadius x:Key="Ui.Radius.Window">12</CornerRadius>
    <Thickness x:Key="Ui.Padding.Window">24</Thickness>
    <Thickness x:Key="Ui.Padding.Button.Small">12,8</Thickness>
    <Thickness x:Key="Ui.Padding.Button.Medium">16,12</Thickness>
    <Thickness x:Key="Ui.Padding.Button.Large">24,16</Thickness>

    <Style x:Key="Ui.Text.WindowTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Ui.Brush.TextPrimary}" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>
    <Style x:Key="Ui.Text.Body" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Ui.Brush.TextSecondary}" />
        <Setter Property="FontSize" Value="16" />
    </Style>

    <ControlTemplate x:Key="Ui.Template.Button" TargetType="Button">
        <Border x:Name="ButtonBorder"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{DynamicResource Ui.Radius.Control}">
            <ContentPresenter Margin="{TemplateBinding Padding}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.75" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="Ui.Button.Primary" TargetType="Button">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource Ui.Brush.WindowRaised}" />
        <Setter Property="Foreground" Value="{DynamicResource Ui.Brush.TextAccent}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Ui.Brush.TextAccent}" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template" Value="{StaticResource Ui.Template.Button}" />
    </Style>

    <Style x:Key="Ui.Button.Secondary" TargetType="Button" BasedOn="{StaticResource Ui.Button.Primary}">
        <Setter Property="Background" Value="{DynamicResource Ui.Brush.Window}" />
        <Setter Property="Foreground" Value="{DynamicResource Ui.Brush.TextSecondary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Ui.Brush.Header}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>
</ResourceDictionary>
