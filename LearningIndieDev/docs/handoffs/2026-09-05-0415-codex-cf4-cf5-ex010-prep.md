# CF-4/CF-5 integration and EX-010 preparation

**Date:** 2026-09-05
**Owner:** Codex
**Branch:** `NF/ConsecutiveRuns`
**Baseline:** `38a5addf` (`Refresh loose-end checkpoint references`)

## What changed

- Added phase-scoped telemetry windows, acquisition timing, and versioned phase
  result serialization to the continuous run domain and both report producers.
- Added boundary checkpoint copy/restore and deterministic runner resume, with
  fork isolation covered by runtime tests.
- Added an opt-in headless continuation schedule. It requires both
  `-PhaseLengthTicks` and `-PhaseUpgradeSchedule`; each semicolon-delimited
  entry is the cumulative loadout for that phase. Existing `CellSim Run`
  commands remain fresh-window runs by default.
- Direct ForestEdge/Hare experimental runs now emit and validate `statline.csv`;
  worker packaging reuses that output. The licensing context probe now cleans
  only helper clients started by its own invocation when the probe fails.
- Prepared the EX-010 draft contract. EX-010 was not executed.

## Verification

- Unity EditMode: `210/210` passed (`artifacts/unity-tests-20260905-040622`).
- Unity PlayMode: `17/18` passed, one intentional graphics-only skip
  (`artifacts/unity-tests-20260905-041257/PlayMode-results.xml`).
- Fresh ForestEdge/Hare schema-25 report, seeds 106–107, 40 ticks: direct
  Stat-Line CSV emitted; strict artifact bundle `VALID`
  (`artifacts/cellular-experiment-20260905-035353`).
- Fresh two-phase `none;none` schedule, seeds 106–107, 40 ticks per phase:
  two phase windows per run, report generation and strict bundle `VALID`
  (`artifacts/cellular-experiment-20260905-040741`).
- Fresh three-phase generic loadout schedule
  `none;stronger-block;stronger-block-2`, seeds 106–107, 40 ticks per phase:
  three phase windows and two boundary acquisitions per run, report generation
  and strict bundle `VALID` (`artifacts/cellular-experiment-20260905-040925`).
- `git diff --check` is clean.

## Decisions and limits

- These schedule runs are integration smoke tests, not EX-010 evidence.
- EX-010 remains gated on a human-approved contract, authored intervention
  schedule/lineage, final Stat-Line meaning review, and the final M1 review.
- The current checkpoint restore is a boundary research handoff, not a player
  save/load format. No historical report was rewritten.
- Existing Unity licensing clients visible before a run are intentionally not
  terminated; only clients created by the current context probe or Unity batch
  invocation are candidates for cleanup.

## Next step

Review and approve/revise `docs/Research/Experiments/EX-010-Sequential-Upgrade-Continuation/CONTRACT_DRAFT.md` before any contract-specific schedule is run. Reconcile the corresponding sprint/board cards after this branch checkpoint is committed.
