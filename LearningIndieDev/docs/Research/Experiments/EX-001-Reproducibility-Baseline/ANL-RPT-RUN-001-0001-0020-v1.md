# ANL-RPT-RUN-001-0001-0020-v1 - AI analysis

**Experiment:** `EXP-001`  
**Analysis ID:** `ANL-RPT-RUN-001-0001-0020-v1`  
**Analysis type:** AI-generated interpretation and suggested actions  
**Status:** Awaiting human decision  
**Source report:** [RPT-RUN-001-0001-0020.md](RPT-RUN-001-0001-0020.md)

This file is analysis, not a replacement for the factual report. Claims below
must remain tied to the recorded evidence and should be revised if a fresh
current-code run disagrees.

## Findings

### 1. The historical pair supports deterministic machine-readable outcomes

**Confidence:** High for the captured pair; provisional for the current code.

The two reports use the same scenario, configuration, seed range, player species,
and ruleset fingerprint. All 20 normalized run payloads and the normalized final
population summaries match. This is stronger evidence than comparing final
counts alone because the population histories and run-level fields were included
in the comparison.

### 2. Raw report hashes are expected to differ

**Confidence:** High.

The raw JSON files contain generated timestamps and output paths, so their file
hashes differ even though their normalized outcome payloads match. Future report
automation should expose a canonical outcome hash or a documented normalization
step so reviewers do not mistake provenance differences for simulation drift.

### 3. The historical fixture has useful variation for later diagnosis

**Confidence:** High, descriptive only.

Across the 20 seeds, final hare population ranges from `248` to `345`, and final
fox population ranges from `0` to `21`. Foxes are extinct in `3/20` runs while
hares are extinct in `0/20`. This makes seed `10116` a useful boundary case and
seed `10102` a reasonable representative case. These observations do not prove
why the outcomes occurred.

### 4. EX-001 does not establish causal explanations

**Confidence:** High.

The report shows outcomes and telemetry, but reproducibility alone cannot tell us
whether food pressure, movement, predation, reproduction, or another rule caused
the lower hare outcomes. That question belongs to EX-002 and requires controlled
interventions.

### 5. Replay fidelity is still an open instrument question

**Confidence:** High.

The source report contains enough metadata to request a replay, but this package
does not include a fresh replay manifest or visual/state comparison. A current
code rerun must close this gap before EX-001 is marked fully passed.

## Suggested actions

1. When Unity is closed, run the exact EX-001 matrix twice using the current
   runner and record the current report schema and fingerprint.
2. Generate a canonical outcome hash from the normalized run payload and final
   summary; retain raw report hashes only as artifact provenance.
3. Replay `10102` and `10116` from the fresh report and attach the replay
   manifest and visual/state result to this package.
4. If the fresh rerun matches, record a human decision that EX-001 passed with
   any known limitations explicitly listed.
5. Only then draft or execute EX-002. Do not change balance parameters as part of
   EX-001.

## Human decision record

**Decision ID:** `DEC-EXP-001-0001`  
**Decision:** Pending  
**Owner:** Pending  
**Date:** Pending  
**Rationale:** Pending human review of the factual report and current-code gate.

## Prohibited inference

The data does not justify saying that foxes caused the lower hare outcomes,
that the ForestEdge rules are balanced, or that the workflow is technically
novel. Those are separate hypotheses requiring their own evidence and review.
