# ANL-RPT-RUN-001-0003-0020-v1 - Current-code gate analysis

**Experiment:** `EXP-001`  
**Analysis ID:** `ANL-RPT-RUN-001-0003-0020-v1`  
**Analysis type:** AI-generated interpretation of a failed execution attempt  
**Status:** Revise and rerun recommended  
**Source report:** [RPT-RUN-001-0003-0020.md](RPT-RUN-001-0003-0020.md)

## Executive summary

The current-code EX-001 matrix did not reach simulation execution. Unity
6000.4.6f1 terminated during native batch startup with exit code `-2147483645`
and did not write a JSON report, CSV, or Unity log. The result is therefore an
instrumentation gap classified as `Underdetermined`, not evidence of
nondeterminism and not a current-code pass.

## Findings

1. **The superseded pair was not current-code evidence.** Its schema-2
   provenance facts were transferred to the current EX-001 record and its
   source artifacts were removed. They cannot substitute for a current-code
   rerun.
2. **The failure is upstream of the experiment.** No seed, ruleset fingerprint,
   population history, or simulation tick was observed, so no ecological
   interpretation is justified from this attempt.
3. **The replay gate cannot start.** Without a fresh report, seeds `10102` and
   `10116` cannot be replayed under current-code provenance.

## Recommended human decision

**Decision:** Revise and rerun  
**Key observation:** The execution environment failed before producing the
current-code evidence required by the protocol.  
**Scope:** Resolve the Unity batch-startup issue or use a known-good clean
environment, then repeat the exact matrix twice and perform both replay checks.
No EX-002 balance or causal claim is authorized.

## Confidence and uncertainty

Confidence is high that this attempt did not produce a simulation report,
because the artifact directory is empty and Unity returned a native failure
code. Confidence is not assignable to the determinism question from this run.
The next run must preserve the editor version, commit, scenario path, ruleset
fingerprint, and tool versions so that the environment change is attributable.
